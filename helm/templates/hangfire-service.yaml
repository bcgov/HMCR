apiVersion: v1
kind: Service
metadata:
  name: hangfire{{ .Values.suffix }}
  labels:
    app: hangfire{{ .Values.suffix }}
spec:
  selector:
    app: hangfire{{ .Values.suffix }}
  ports:
  - name: web
    port: 8080
    targetPort: web
    protocol: TCP
