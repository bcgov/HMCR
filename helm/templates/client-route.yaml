apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: client-{{ .Values.env | lower }}
  labels:
    app: client{{ .Values.suffix }}
  annotations:
    haproxy.router.openshift.io/timeout: 1h
spec:
  host: {{ .Values.host }}
  path: /
  to:
    kind: Service
    name: client{{ .Values.suffix }}
    weight: 100
  port:
    targetPort: web
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
